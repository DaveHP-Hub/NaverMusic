import { DomplayerspotifyPipe } from './domplayerspotify.pipe';

describe('DomplayerspotifyPipe', () => {
  it('create an instance', () => {
    const pipe = new DomplayerspotifyPipe();
    expect(pipe).toBeTruthy();
  });
});
